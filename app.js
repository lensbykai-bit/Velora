const media=[{title:'Neon City Pack',type:'PHOTO',price:'$6.99'},{title:'Creator Motion Set',type:'VIDEO',price:'$12.99'},{title:'Travel Stories',type:'PHOTO',price:'$8.50'},{title:'Cinematic Reels',type:'VIDEO',price:'$15.00'}];
const creators=[['AR','Aria Rose','128 items'],['MK','Miko','84 items'],['LV','Livia','211 items']];
const grid=document.getElementById('trendingGrid');
const creatorRow=document.getElementById('creatorRow');
const main=document.getElementById('mainContent');
const modal=document.getElementById('modal');
const modalCard=document.getElementById('modalCard');
function renderHome(){location.reload()}
media.forEach((m,i)=>{const c=document.createElement('button');c.className='media-card';c.innerHTML=`<span class="pill">${m.type}</span><h3>${m.title}</h3><div class="price">${m.price}</div>`;c.onclick=()=>openProduct(m);grid.appendChild(c)});
creators.forEach(c=>{const el=document.createElement('div');el.className='creator';el.innerHTML=`<div class="avatar">${c[0]}</div><strong>${c[1]}</strong><small>${c[2]}</small>`;creatorRow.appendChild(el)});
function openProduct(m){modalCard.innerHTML=`<h2>${m.title}</h2><p class="muted">Premium ${m.type.toLowerCase()} asset from a VELORA creator.</p><div style="height:120px;border-radius:18px;background:linear-gradient(135deg,#3c1a50,#171020);margin:16px 0"></div><h3>${m.price}</h3><button class="btn primary" style="width:100%">Add to Purchases</button><button class="btn ghost" style="width:100%;margin-top:10px" onclick="closeModal()">Close</button>`;modal.classList.remove('hidden')}
function closeModal(){modal.classList.add('hidden')}window.closeModal=closeModal;modal.addEventListener('click',e=>{if(e.target===modal)closeModal()});
function setActive(name){document.querySelectorAll('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.nav===name))}
function page(title,body){main.innerHTML=`<section class="section" style="margin-top:26px"><div class="section-head"><h2>${title}</h2></div>${body}</section>`}
function navigate(name){setActive(name);if(name==='home')return renderHome();if(name==='explore')page('Explore',`<div class="tabs"><button class="tab active">Photos</button><button class="tab">Videos</button></div><div class="media-grid">${media.map(m=>`<div class="media-card"><span class="pill">${m.type}</span><h3>${m.title}</h3><div class="price">${m.price}</div></div>`).join('')}</div>`);if(name==='upload')page('Upload & Sell',`<div class="safety-card" style="margin-bottom:16px"><div class="shield">✓</div><div><h3>Publish responsibly</h3><p>Upload only media you created or have rights to sell.</p></div></div><label>Title</label><input class="field" placeholder="Name your content"><label>Type</label><select class="field"><option>Photo</option><option>Video</option></select><label>Price</label><input class="field" placeholder="$0.00"><button class="btn primary" style="width:100%">Choose File</button>`);if(name==='purchases')page('Purchases',`<div class="empty">Your purchased photo and video items will appear here.</div>`);if(name==='profile')page('Profile',`<div style="text-align:center;padding:18px"><div class="avatar" style="margin:0 auto 12px;width:72px;height:72px">V</div><h2 style="margin:0">VELORA Creator</h2><p class="muted">Manage your portfolio, sales and account.</p><button class="btn primary">Edit Profile</button></div>`)}
document.querySelectorAll('[data-nav]').forEach(b=>b.addEventListener('click',()=>navigate(b.dataset.nav)));
document.getElementById('searchBtn').onclick=()=>{modalCard.innerHTML=`<h2>Search VELORA</h2><input class="field" autofocus placeholder="Search photos, videos, creators"><button class="btn primary" style="width:100%">Search</button><button class="btn ghost" style="width:100%;margin-top:10px" onclick="closeModal()">Close</button>`;modal.classList.remove('hidden')};
